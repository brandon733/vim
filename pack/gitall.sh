##  -- GENERATED FILE
[ -d "$packdir/plugins-color/start/colors-modern-borland" ] || { mkdir -p "$packdir/plugins-color/start/colors-modern-borland" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/letorbi/vim-colors-modern-borland.git colors-modern-borland; } ## plugins-color/start/colors-modern-borland
[ -d "$packdir/plugins-color/start/colors-pencil" ] || { mkdir -p "$packdir/plugins-color/start/colors-pencil" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/reedes/vim-colors-pencil.git colors-pencil; } ## plugins-color/start/colors-pencil
[ -d "$packdir/plugins-color/start/gruvbox" ] || { mkdir -p "$packdir/plugins-color/start/gruvbox" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/morhetz/gruvbox.git gruvbox; } ## plugins-color/start/gruvbox
[ -d "$packdir/plugins-color/start/gruvbox8" ] || { mkdir -p "$packdir/plugins-color/start/gruvbox8" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/lifepillar/gruvbox8.git gruvbox8; } ## plugins-color/start/gruvbox8
[ -d "$packdir/plugins-color/start/iceberg" ] || { mkdir -p "$packdir/plugins-color/start/iceberg" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/cocopon/iceberg.vim.git iceberg; } ## plugins-color/start/iceberg
[ -d "$packdir/plugins-color/start/molokai" ] || { mkdir -p "$packdir/plugins-color/start/molokai" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/tomasr/molokai.git molokai; } ## plugins-color/start/molokai
[ -d "$packdir/plugins-color/start/nord" ] || { mkdir -p "$packdir/plugins-color/start/nord" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/arcticicestudio/nord-vim.git nord; } ## plugins-color/start/nord
[ -d "$packdir/plugins-color/start/oceanic-next" ] || { mkdir -p "$packdir/plugins-color/start/oceanic-next" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/mhartington/oceanic-next.git oceanic-next; } ## plugins-color/start/oceanic-next
[ -d "$packdir/plugins-color/start/papercolor-theme" ] || { mkdir -p "$packdir/plugins-color/start/papercolor-theme" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/NLKNguyen/papercolor-theme.git papercolor-theme; } ## plugins-color/start/papercolor-theme
[ -d "$packdir/plugins-color/start/selenized" ] || { mkdir -p "$packdir/plugins-color/start/selenized" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/jan-warchol/selenized.git selenized; } ## plugins-color/start/selenized
[ -d "$packdir/plugins-color/start/solarized8" ] || { mkdir -p "$packdir/plugins-color/start/solarized8" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/lifepillar/vim-solarized8.git solarized8; } ## plugins-color/start/solarized8
[ -d "$packdir/plugins-color/start/summerfruit256" ] || { mkdir -p "$packdir/plugins-color/start/summerfruit256" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/mightwork/summerfruit256.vim.git summerfruit256; } ## plugins-color/start/summerfruit256
[ -d "$packdir/plugins-color/start/tomorrow-theme" ] || { mkdir -p "$packdir/plugins-color/start/tomorrow-theme" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/chriskempson/tomorrow-theme.git tomorrow-theme; } ## plugins-color/start/tomorrow-theme
[ -d "$packdir/plugins-color/start/xterm-color-table" ] || { mkdir -p "$packdir/plugins-color/start/xterm-color-table" && cd "$packdir/plugins-color/start" && git clone --recursive https://github.com/guns/xterm-color-table.vim.git xterm-color-table; } ## plugins-color/start/xterm-color-table
[ -d "$packdir/plugins-syn/start/ale" ] || { mkdir -p "$packdir/plugins-syn/start/ale" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/w0rp/ale.git ale; } ## plugins-syn/start/ale
[ -d "$packdir/plugins-syn/start/commentary" ] || { mkdir -p "$packdir/plugins-syn/start/commentary" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/tpope/vim-commentary.git commentary; } ## plugins-syn/start/commentary
[ -d "$packdir/plugins-syn/start/fastfold" ] || { mkdir -p "$packdir/plugins-syn/start/fastfold" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/Konfekt/FastFold.git fastfold; } ## plugins-syn/start/fastfold
[ -d "$packdir/plugins-syn/start/graphql" ] || { mkdir -p "$packdir/plugins-syn/start/graphql" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/jparise/vim-graphql.git graphql; } ## plugins-syn/start/graphql
[ -d "$packdir/plugins-syn/start/isort" ] || { mkdir -p "$packdir/plugins-syn/start/isort" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/brentyi/isort.vim.git isort; } ## plugins-syn/start/isort
[ -d "$packdir/plugins-syn/start/javascript" ] || { mkdir -p "$packdir/plugins-syn/start/javascript" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/pangloss/vim-javascript.git javascript; } ## plugins-syn/start/javascript
[ -d "$packdir/plugins-syn/start/kotlin" ] || { mkdir -p "$packdir/plugins-syn/start/kotlin" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/udalov/kotlin-vim.git kotlin; } ## plugins-syn/start/kotlin
[ -d "$packdir/plugins-syn/start/lsp" ] || { mkdir -p "$packdir/plugins-syn/start/lsp" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/yegappan/lsp.git lsp; } ## plugins-syn/start/lsp
[ -d "$packdir/plugins-syn/start/markdown" ] || { mkdir -p "$packdir/plugins-syn/start/markdown" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/plasticboy/vim-markdown markdown; } ## plugins-syn/start/markdown
[ -d "$packdir/plugins-syn/start/pgsql" ] || { mkdir -p "$packdir/plugins-syn/start/pgsql" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/lifepillar/pgsql.vim.git pgsql; } ## plugins-syn/start/pgsql
[ -d "$packdir/plugins-syn/start/plantuml-syntax" ] || { mkdir -p "$packdir/plugins-syn/start/plantuml-syntax" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/aklt/plantuml-syntax.git plantuml-syntax; } ## plugins-syn/start/plantuml-syntax
[ -d "$packdir/plugins-syn/start/prettier" ] || { mkdir -p "$packdir/plugins-syn/start/prettier" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/prettier/vim-prettier.git prettier; } ## plugins-syn/start/prettier
[ -d "$packdir/plugins-syn/start/protobuf" ] || { mkdir -p "$packdir/plugins-syn/start/protobuf" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/uarun/vim-protobuf.git protobuf; } ## plugins-syn/start/protobuf
[ -d "$packdir/plugins-syn/start/snippets" ] || { mkdir -p "$packdir/plugins-syn/start/snippets" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/honza/vim-snippets.git snippets; } ## plugins-syn/start/snippets
[ -d "$packdir/plugins-syn/start/yats" ] || { mkdir -p "$packdir/plugins-syn/start/yats" && cd "$packdir/plugins-syn/start" && git clone --recursive https://github.com/HerringtonDarkholme/yats.vim yats; } ## plugins-syn/start/yats
[ -d "$packdir/plugins-ui/start/highlighter" ] || { mkdir -p "$packdir/plugins-ui/start/highlighter" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/azabiong/vim-highlighter.git highlighter; } ## plugins-ui/start/highlighter
[ -d "$packdir/plugins-ui/start/lightline" ] || { mkdir -p "$packdir/plugins-ui/start/lightline" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/itchyny/lightline.vim.git lightline; } ## plugins-ui/start/lightline
[ -d "$packdir/plugins-ui/start/sneak" ] || { mkdir -p "$packdir/plugins-ui/start/sneak" && cd "$packdir/plugins-ui/start" && git clone --recursive https://github.com/justinmk/vim-sneak.git sneak; } ## plugins-ui/start/sneak
[ -d "$packdir/plugins-util/start/abolish" ] || { mkdir -p "$packdir/plugins-util/start/abolish" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-abolish.git abolish; } ## plugins-util/start/abolish
[ -d "$packdir/plugins-util/start/ack" ] || { mkdir -p "$packdir/plugins-util/start/ack" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/mileszs/ack.vim.git ack; } ## plugins-util/start/ack
[ -d "$packdir/plugins-util/start/clam" ] || { mkdir -p "$packdir/plugins-util/start/clam" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/sjl/clam.vim.git clam; } ## plugins-util/start/clam
[ -d "$packdir/plugins-util/start/csv" ] || { mkdir -p "$packdir/plugins-util/start/csv" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/chrisbra/csv.vim.git csv; } ## plugins-util/start/csv
[ -d "$packdir/plugins-util/start/dadbod" ] || { mkdir -p "$packdir/plugins-util/start/dadbod" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-dadbod.git dadbod; } ## plugins-util/start/dadbod
[ -d "$packdir/plugins-util/start/dadbod-ui" ] || { mkdir -p "$packdir/plugins-util/start/dadbod-ui" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/kristijanhusak/vim-dadbod-ui.git dadbod-ui; } ## plugins-util/start/dadbod-ui
[ -d "$packdir/plugins-util/start/dirdiff" ] || { mkdir -p "$packdir/plugins-util/start/dirdiff" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/will133/vim-dirdiff.git dirdiff; } ## plugins-util/start/dirdiff
[ -d "$packdir/plugins-util/start/expand-region" ] || { mkdir -p "$packdir/plugins-util/start/expand-region" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/terryma/vim-expand-region.git expand-region; } ## plugins-util/start/expand-region
[ -d "$packdir/plugins-util/start/fugitive" ] || { mkdir -p "$packdir/plugins-util/start/fugitive" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-fugitive.git fugitive; } ## plugins-util/start/fugitive
[ -d "$packdir/plugins-util/start/fzf" ] || { mkdir -p "$packdir/plugins-util/start/fzf" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/junegunn/fzf.vim.git fzf; } ## plugins-util/start/fzf
[ -d "$packdir/plugins-util/start/gitgutter" ] || { mkdir -p "$packdir/plugins-util/start/gitgutter" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/airblade/vim-gitgutter.git gitgutter; } ## plugins-util/start/gitgutter
[ -d "$packdir/plugins-util/start/localvimrc" ] || { mkdir -p "$packdir/plugins-util/start/localvimrc" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/embear/vim-localvimrc.git localvimrc; } ## plugins-util/start/localvimrc
[ -d "$packdir/plugins-util/start/matchit" ] || { mkdir -p "$packdir/plugins-util/start/matchit" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tmhedberg/matchit.git matchit; } ## plugins-util/start/matchit
[ -d "$packdir/plugins-util/start/mergetool" ] || { mkdir -p "$packdir/plugins-util/start/mergetool" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/samoshkin/vim-mergetool.git mergetool; } ## plugins-util/start/mergetool
[ -d "$packdir/plugins-util/start/repeat" ] || { mkdir -p "$packdir/plugins-util/start/repeat" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-repeat.git repeat; } ## plugins-util/start/repeat
[ -d "$packdir/plugins-util/start/rest-console" ] || { mkdir -p "$packdir/plugins-util/start/rest-console" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/diepm/vim-rest-console.git rest-console; } ## plugins-util/start/rest-console
[ -d "$packdir/plugins-util/start/splitjoin" ] || { mkdir -p "$packdir/plugins-util/start/splitjoin" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/AndrewRadev/splitjoin.vim.git splitjoin; } ## plugins-util/start/splitjoin
[ -d "$packdir/plugins-util/start/surround" ] || { mkdir -p "$packdir/plugins-util/start/surround" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-surround.git surround; } ## plugins-util/start/surround
[ -d "$packdir/plugins-util/start/tig-explorer" ] || { mkdir -p "$packdir/plugins-util/start/tig-explorer" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/iberianpig/tig-explorer.vim.git tig-explorer; } ## plugins-util/start/tig-explorer
[ -d "$packdir/plugins-util/start/undotree" ] || { mkdir -p "$packdir/plugins-util/start/undotree" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/mbbill/undotree.git undotree; } ## plugins-util/start/undotree
[ -d "$packdir/plugins-util/start/unimpaired" ] || { mkdir -p "$packdir/plugins-util/start/unimpaired" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-unimpaired.git unimpaired; } ## plugins-util/start/unimpaired
[ -d "$packdir/plugins-util/start/vinegar" ] || { mkdir -p "$packdir/plugins-util/start/vinegar" && cd "$packdir/plugins-util/start" && git clone --recursive https://github.com/tpope/vim-vinegar.git vinegar; } ## plugins-util/start/vinegar
